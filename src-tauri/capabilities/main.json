{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Only the bundled main window may read non-sensitive bootstrap metadata.",
  "windows": ["main"],
  "permissions": ["allow-get-bootstrap-info"]
}
